;;; packages.el --- aiden-scss Layer packages File for Spacemacs
(setq aiden-scss-packages
    '(
      scss-mode
      ))

;; List of packages to exclude.
(setq aiden-scss-excluded-packages '())

;; For each package, define a function aiden-scss/init-<package-name>
;;
;; (defun aiden-scss/init-my-package ()
;;   "Initialize my package"
;;   )
;;
;; Often the body of an initialize function uses `use-package'
;; For more info on `use-package', see readme:
;; https://github.com/jwiegley/use-package
