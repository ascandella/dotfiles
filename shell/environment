# Go language specific settings

for dir in '.gocode' 'src/gocode' ; do
  if [ -d "$HOME/$dir" ]; then
    export GOPATH="$HOME/$dir"

    export PATH="$PATH:$HOME/$dir/bin"
  fi
done

# Enable e.g. `ls` colors
export CLICOLOR=1
